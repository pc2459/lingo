extends layout

block content
  .container
    form(id="translate", method="post", action="/get-quiz")
    label(for="fromlanguage") Choose Your Language
    select(id="fromlanguage")
      for language in languages
        option(value="language") language
    input(type="submit")